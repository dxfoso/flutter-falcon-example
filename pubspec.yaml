name: red_rect_app
description: Minimal Flutter Falcon test app.
publish_to: 'none'
version: 1.1.6+10

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_falcon:
    path: ../..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
