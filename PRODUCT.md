# Product

A compact reference app showing how an application keeps its own UI while the
single FlutterFalcon package controls update state and actions.

The screen must expose the effective local/live app server, standard or
FlutterFalcon build mode, artifact type, patch state, automatic-update choice,
and the next valid update action without decorative or duplicate controls.
